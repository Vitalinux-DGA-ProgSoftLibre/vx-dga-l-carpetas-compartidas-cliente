#!/bin/sh
# preinst script for vx-dga-l-carpetas-compartidas-cliente
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <new-preinst> `install'
#        * <new-preinst> `install' <old-version>
#        * <new-preinst> `upgrade' <old-version>
#        * <old-preinst> `abort-upgrade' <new-version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
	install|upgrade)
	## Eliminamos del fstab los puntos de montaje NFS y cualquier línea en blanco que hubiera
	## Limpiamos el /etc/fstab
	sed -i "/^#/d" /etc/fstab
	sed -i "/^$/d" /etc/fstab
	sed -i "/.*\/nfs\/alumnos.*/d" /etc/fstab
	sed -i "/.*\/nfs\/profesor.*/d" /etc/fstab
	sed -i "/.*\/nfs\/privado.*/d" /etc/fstab
	sed -i "/.*\/nfs\/perfiles.*/d" /etc/fstab

	;;

    abort-upgrade)
    ;;

    *)
        echo "preinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.



exit 0
